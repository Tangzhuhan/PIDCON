from setuptools import setup

setup(
    name="PID Temperature Control System",
    version="1.0",
    install_requires=[
        'pyserial',
        'pyqtgraph',
        'pandas',
        'PyQt5',
    ],
) 